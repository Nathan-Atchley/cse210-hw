Program can be found in final/FinalProject
When you launch the program it will run in the console.
Most everything should be pretty self explanitory. 
The only not obvious thing is that you must enter a valid pokemon name
if you are running in VSCode or a similar IDE, just push continue through the errors and the program will continue running. It is a setting in the IDE that raises those exceptions anyway, even though there is a try {} catch{} block that intercepts the error.